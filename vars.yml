minibase_ssh_private_key: Deploy.key
ranger_user: { "user": "minibase", "group": "minibase", "password": "minibasePWD", "uid": "1040" }
format_confirm: "{{ format_force | default('no') }}"
data_disks:
  - /dev/sdb
  - /dev/sdc
home_link: no
home_link_dir: /data0{{ (data_disks | length) -1 }}
jumper_host: "{{ groups['minibase'][0] }}"
timezone: Asia/Shanghai
lvm_enable: no
vg_name: data
lv_name: pool
